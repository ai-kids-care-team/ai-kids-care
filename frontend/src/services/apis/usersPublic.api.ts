/** User profile reads remain closed until authenticated self/admin contracts exist. */
export async function getLoginIdByUserId(userId: number): Promise<string | null> {
  void userId;
  return null;
}
