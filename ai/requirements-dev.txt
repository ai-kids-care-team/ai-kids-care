# Test / dev toolchain for ai/ (declared per ADR/assessment N-5 review P2).
# Install on top of the runtime deps once those are pinned to real versions:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Note: requirements.txt currently carries placeholder future version pins
# (e.g. torch==2.11.0+cu130) that must be resolved against the real CUDA
# environment before a clean install works — tracked as a separate follow-up.
# The serving tests (ai/tests/test_serving.py) run with just pytest + httpx +
# fastapi (predictor mocked, no av/torch); the av-dependent tests
# (test_sample_frame_indices, test_persistence) need the full runtime env.
pytest>=8.0.0
httpx>=0.27.0
