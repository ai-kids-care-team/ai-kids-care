type: object
required:
  - childId
  - classId
  - startDate
properties:
  childId:
    type: integer
    format: int64
  classId:
    type: integer
    format: int64
  startDate:
    type: string
    format: date
  endDate:
    type: string
    format: date
    nullable: true
  reason:
    type: string
    nullable: true
  note:
    type: string
    nullable: true
  status:
    $ref: ./StatusEnum.yaml
