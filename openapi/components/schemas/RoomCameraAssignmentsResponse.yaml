type: object
properties:
  assignment_id:
    type: integer
    format: int64
  kindergarten_id:
    type: integer
    format: int64
  camera_id:
    type: integer
    format: int64
  room_id:
    type: integer
    format: int64
  start_at:
    type: string
    format: date-time
    description: 설치/배치 시작 일시
  end_at:
    type: string
    format: date-time
    description: 설치/배치 종료 일시(철거/이동 시, NULL=현재 유효)
  created_at:
    type: string
    format: date-time
    description: 생성 일시
  updated_at:
    type: string
    format: date-time
    description: 수정 일시
required:
  - assignment_id
  - kindergarten_id
  - camera_id
  - room_id
  - start_at
