type: object
required:
  - classId
  - teacherId
  - startDate
properties:
  classId:
    type: integer
    format: int64
  teacherId:
    type: integer
    format: int64
  role:
    type: string
    nullable: true
  startDate:
    type: string
    format: date
  endDate:
    type: string
    format: date
    nullable: true
  reason:
    type: string
    nullable: true
  note:
    type: string
    nullable: true
  status:
    $ref: ./StatusEnum.yaml
