type: object
properties:
  loginId:
    type: string
    nullable: true
  email:
    type: string
    format: email
    nullable: true
  phone:
    type: string
    nullable: true
  status:
    $ref: ./StatusEnum.yaml
description: |
  Typically only admins can change status; server enforces authorization.
