type: object
required:
  - cameraId
  - kindergartenId
  - status
  - createdAt
  - updatedAt
properties:
  cameraId:
    type: integer
    format: int64
  kindergartenId:
    type: integer
    format: int64
  serialNo:
    type: string
    nullable: true
  cameraName:
    type: string
    nullable: true
  model:
    type: string
    nullable: true
  createdByUserId:
    type: integer
    format: int64
    nullable: true
  status:
    $ref: ./StatusEnum.yaml
  lastSeenAt:
    type: string
    format: date-time
    nullable: true
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
