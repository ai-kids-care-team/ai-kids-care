type: object
required:
  - cameraId
  - roomId
  - startAt
properties:
  cameraId:
    type: integer
    format: int64
  roomId:
    type: integer
    format: int64
  startAt:
    type: string
    format: date-time
  endAt:
    type: string
    format: date-time
    nullable: true
