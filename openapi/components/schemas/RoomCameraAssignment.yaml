type: object
required:
  - assignmentId
  - kindergartenId
  - cameraId
  - roomId
  - startAt
  - createdAt
  - updatedAt
properties:
  assignmentId:
    type: integer
    format: int64
  kindergartenId:
    type: integer
    format: int64
  cameraId:
    type: integer
    format: int64
  roomId:
    type: integer
    format: int64
  startAt:
    type: string
    format: date-time
  endAt:
    type: string
    format: date-time
    nullable: true
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
