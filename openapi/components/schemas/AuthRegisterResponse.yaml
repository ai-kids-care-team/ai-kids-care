type: object
required:
  - userId
  - status
  - createdAt
properties:
  userId:
    type: integer
    format: int64
  status:
    $ref: ./StatusEnum.yaml
  createdAt:
    type: string
    format: date-time
