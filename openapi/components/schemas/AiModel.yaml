type: object
required:
  - modelId
  - name
  - version
  - status
  - createdAt
  - updatedAt
properties:
  modelId:
    type: integer
    format: int64
  name:
    type: string
  version:
    type: string
  status:
    $ref: ./StatusEnum.yaml
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
