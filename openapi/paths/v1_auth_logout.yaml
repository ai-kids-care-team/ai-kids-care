post:
  tags:
    - Auth
  security: []
  summary: Logout (revoke refresh token)
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../components/schemas/AuthLogoutRequest.yaml
  responses:
    '204':
      description: No Content
