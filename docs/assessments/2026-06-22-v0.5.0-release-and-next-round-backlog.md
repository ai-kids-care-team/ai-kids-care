---
type: release-record-and-backlog
date: 2026-06-22
status: Active
based_on: 2026-06-18-followup-backlog.md
---

# v0.5.0 发布记录 + 下一轮结转 Backlog

本文件做两件事:① 记录 **v0.5.0 已发布上线**;② 把上一轮收尾后**结转到下一轮**的工作项**耐久归档**(此前仅存于本地、已忽略的 `.ai/CHECKPOINT.local.md`)。重启时从本文件 §下一轮 Backlog 起步。

`2026-06-18-followup-backlog.md` 的全部波次已 **Done 并发布**,该文件转为历史快照。

## 发布事实(Release Facts)

- **版本**:`v0.5.0`,tag 指向 `main @ 0d94aa2`。
- **部署**:GitHub Actions `release.yml` run 27931050026 首发因冒烟缺 fail-fast compose 变量失败 → 修复(`ebb9693` / PR #101)后 run 27931589967 **build-smoke success + Promote :version→:prod success**,watchtower 拉 `:prod` 上线。
- **本轮交付**(对应 `2026-06-18-followup-backlog.md`):SEC-0026 摄像头流密码 AES-256-GCM 加密链路(ADR-0026)、BE-5 感谢信读写端点(SPEC-0003/ADR-0028)、FE-2 前端感谢信簇重激活、BE-4/FE-name/FE-1/FE-authorId/FE-3、CI-4(AI pytest 入 CI)、AI-2(uv.lock + `uv sync --frozen`)、Wave 0-3 安全加固(RRN 历史清除、休眠 service denyAll、VO 收窄、AN-READ 公告平台级读、BE-1 全局异常处理、CODEGEN 退役)。
- **维护者手动项(已完成)**:SEC-0026 部署密钥注入(`CAMERA_STREAM_AES_KEY_V1` 仅 Java、`AI_SERVICE_TOKEN` 两端同值、AI 端 `JAVA_BACKEND_URL`);`main`/`develop` force-push 保护已开启;CI-3 决定维持现状(单人开发,不加 enforce_admins/强制审批;main 保留 4 必需 checks + 禁 force-push/删除)。
- **分支**:`feat/notification-sms-email` 已删除(旧 SMS/email 功能弃用,日后重建);远端仅 `main` + `develop`。

## 下一轮 Backlog(结转项,重启起点)

> 均**非阻塞**。重启时建议先做 NR-0(重新审计)再排波,或直接挑 NR-1。

| ID | 描述 | 类型 | 优先级 | 来源 |
| --- | --- | --- | --- | --- |
| NR-0 | **重新出一份 dated 审计**:上一基线为 `2026-06-17-followup-audit.md`,之后改动 207 文件(新公开 API、前端重构、加密链路),基线已显著漂移。重启时先证据审计,系统排出下一波。 | 流程 | 推荐起点 | 项目审计节奏 |
| NR-1 | **notification SMS/email 功能重建**:旧分支已弃,从头按 spec-first 重做(复用 ADR-0018 通知子系统)。最大用户价值缺口。 | 功能 | 高 | 维护者 2026-06-22 决定 |
| NR-2 | **SEC-0025 pepper 轮换实现**:设计完整(ADR-0025 R1-R5),决策推迟(build-on-demand);实施前定 OQ-1~4(强制 vs 懒/休眠账号/端点认证/过渡期)。 | 安全 | 中(择机) | ADR-0025 |
| NR-3 | **ADR-0026 OQ-6**:摄像头密文加 AAD 绑定 `stream_id`(延后的加密加固)。 | 安全 | 中 | ADR-0026 OQ-6 |
| NR-4 | **密钥运维 runbook**:`CAMERA_STREAM_AES_KEY_V1` / `AI_SERVICE_TOKEN` 已注入,缺一份轮换/灾备/key_version 升级流程文档(密钥丢失=该批密文不可解密)。 | 运维 | 中 | SEC-0026 |
| NR-5 | **前端测试缺失**:CI-4 仅接入 AI pytest,前端只有 lint+build、**无测试**。补关键路径(登录/感谢信/公告)测试并接 CI。 | 质量 | 中 | CI-4 范围外 |
| NR-6 | **BE-5 `buildVO` N+1**:`AppreciationLetterService.buildVO` 每封信额外查 guardian+teacher 名;Phase-1 已注释,可改 JOIN 优化。 | 性能 | 低 | SPEC-0003 取舍 |
| NR-7 | **AI cu130/CUDA 13.2 实机核对**:`pyproject.toml` cu130 index 对应 CUDA 13.2 系"推断";在 deploy host 核对 GPU 实际匹配、`uv.lock` torch 版本可跑。 | 运维 | 低 | 2026-06-18 残留 #1 |
| NR-8 | **文档漂移清扫**:rest-endpoints/README/architecture/ADR/spec 在本轮大改后落后(幻影 Controller、JWT→session、感谢信状态、ADR-0027 状态等)。**已于 2026-06-22 处理(见本轮)**。 | 文档 | — | 见下 |

## 已确认无需处理

- `refs/pull/*` 中的改写前旧 RRN(假数据,维护者确认非敏感,不清理)。
- 其它本地 clone 若停在改写前历史(报冲突):`git fetch && git reset --hard origin/<branch>` 或重 clone 对齐(维护者各机自理)。

## 关联

- 历史快照:`docs/assessments/2026-06-18-followup-backlog.md`(全 Done)、`docs/assessments/2026-06-17-followup-audit.md`(上一基线)。
- 决策:ADR-0025(pepper 轮换)、ADR-0026(摄像头加密)、ADR-0028(感谢信端点)。
- 规范:SPEC-0003(感谢信)。
