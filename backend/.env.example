DB_NAME=
DB_USER=
DB_PASSWORD=
PUSHOVER_API_TOKEN=
# Deprecated — replaced by CAMERA_STREAM_AES_KEY_V1 (ADR-0026 Phase 1)
# CAMERA_STREAMS_ENC_KEY=
# CAMERA_STREAMS_ENC_KEY_VERSION=

# AES-256-GCM key for camera stream password encryption (ADR-0026 Phase 1).
# Generate: openssl rand -base64 32
CAMERA_STREAM_AES_KEY_V1=

# Shared Bearer token for the AI service to call the internal stream-credential
# endpoint /api/v1/internal/** (ADR-0026 Phase 2, OQ-1=A). The Java backend and the
# AI service must share the same value. The AES key above stays Java-only.
# Generate: openssl rand -base64 32
AI_SERVICE_TOKEN=
